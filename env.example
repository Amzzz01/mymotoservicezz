# ====================================================================
# MyMotoLog Environment Variables Configuration
# ====================================================================
# 
# IMPORTANT: This file contains PLACEHOLDER values only.
# DO NOT put real API keys in this file as it is committed to GitHub.
#
# To set up your local development environment:
# 1. Copy this file and rename it to `.env`
# 2. Replace all placeholder values with your actual API keys
# 3. The `.env` file is gitignored and will NOT be committed
#
# ====================================================================

# --------------------------------------------------------------------
# Firebase Configuration
# --------------------------------------------------------------------
# Get these values from Firebase Console:
# https://console.firebase.google.com/
# Navigate to: Project Settings > Your apps > SDK setup and configuration
#
# Note: Firebase API keys are designed to be used in client-side code
# and are protected by Firebase Security Rules and App Check.
# --------------------------------------------------------------------

VITE_FIREBASE_API_KEY="your-firebase-api-key-here"
VITE_FIREBASE_AUTH_DOMAIN="your-project-id.firebaseapp.com"
VITE_FIREBASE_PROJECT_ID="your-project-id"
VITE_FIREBASE_STORAGE_BUCKET="your-project-id.firebasestorage.app"
VITE_FIREBASE_MESSAGING_SENDER_ID="your-messaging-sender-id"
VITE_FIREBASE_APP_ID="your-firebase-app-id"

# --------------------------------------------------------------------
# Google Gemini AI API Key
# --------------------------------------------------------------------
# Get your API key from Google AI Studio:
# https://aistudio.google.com/app/apikey
#
# Used for: AI-powered maintenance suggestions based on service history
#
# Setup instructions:
# 1. Visit https://aistudio.google.com/app/apikey
# 2. Click "Create API key"
# 3. Create a new project or select existing one
# 4. Copy the generated API key (starts with "AIzaSy...")
# 5. (Optional) Add API restrictions for security:
#    - Set HTTP referrer restrictions to your domain
#    - Limit to Generative Language API only
# --------------------------------------------------------------------

VITE_GEMINI_API_KEY="your-gemini-api-key-here"

# --------------------------------------------------------------------
# Mindee OCR API Key
# --------------------------------------------------------------------
# Get your API key from Mindee Platform:
# https://platform.mindee.com/
#
# Used for: Receipt scanning and automatic data extraction
#
# Setup instructions:
# 1. Sign up at https://platform.mindee.com/
# 2. Navigate to your profile (top right) > API Keys
# 3. Click "Create new API Key"
# 4. Copy the generated API key (starts with "md_...")
# 5. Free tier includes 250 documents/month
#
# Note: If this API key is not configured, the receipt scanning
# feature will be disabled but the app will continue to function
# with manual entry mode.
# --------------------------------------------------------------------

VITE_MINDEE_API_KEY="your-mindee-api-key-here"

# ====================================================================
# DEPLOYMENT NOTES
# ====================================================================
#
# For Vercel Deployment:
# 1. Go to your Vercel project dashboard
# 2. Navigate to Settings > Environment Variables
# 3. Add each variable above with your actual values
# 4. Select all environments: Production, Preview, Development
# 5. Save and redeploy
#
# Security Best Practices:
# - Never commit real API keys to version control
# - Use different API keys for development and production
# - Rotate API keys regularly
# - Add API restrictions where supported
# - Monitor API usage for unexpected activity
#
# ====================================================================
